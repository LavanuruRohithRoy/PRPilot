[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "prpilot-backend"
version = "0.1.0"
description = "PRPilot backend services"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn>=0.30.0",
]
